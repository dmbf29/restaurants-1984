<%= simple_form_for restaurant do |f| %>
  <%= f.input :name, input_html: { class: 'bg-danger' } %>
  <%= f.input :address %>
  <%= f.input :rating, collection: [1, 2, 3, 4, 5] %>
  <%= f.input :category %>
  <%= f.submit "Submit", class: 'btn btn-primary mt-3' %>
<% end %>
